export interface PlaceReviewType {
  id: string;
  authorAvatar: string;
  authorId: string;
  authorName: string;
  createdAt: string;
  images: string[];
  placeId: string;
  rate: number;
  text: string;
}
