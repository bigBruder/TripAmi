export interface PlaceReviewType {
  authorAvatar: string;
  authorId: string;
  authorName: string;
  createdAt: string;
  images: string[];
  placeId: string;
  rate: number;
  text: string;
}
